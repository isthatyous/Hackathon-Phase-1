apiVersion: v1
kind: Service
metadata:
  name: {{ include "online-shop.fullname" . }}
  labels:
    {{- include "online-shop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "online-shop.selectorLabels" . | nindent 4 }}
